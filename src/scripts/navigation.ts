// Keep in sync with the header-only 1199/1200px CSS boundary.
// Page content, hero art direction and sticky CTA still switch at 991/992px.
export const desktopNavigationQuery = '(min-width: 1200px)';
